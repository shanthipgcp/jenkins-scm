pipeline {
    agent any
    stages {
        stage ('Build') {
            steps {
                echo "hi from github scm"
                echo "executing from scm"
            }
        }
    }
}
